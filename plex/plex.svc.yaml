apiVersion: v1
kind: Service
metadata:
  name: plex
  labels:
    app: plex
spec:
  selector:
    app: plex
  type: NodePort
  ports:
    - name: plex
      port: 32400
      targetPort: 32400
  externalIPs:
    - 192.168.1.150
